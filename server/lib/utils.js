// const mongoose = require("mongoose");

// const mongoDBURL = process.env.MONGO_URI; // Assuming MongoDB URI is stored in an environment variable

//   mongoose
//     .connect(mongoDBURL)
//     .then(() => {
//       console.log("Connected to MongoDB");
//     })
//     .catch((error) => {
//       console.error("Error connecting to MongoDB:", error);
//       // Handle the error, e.g., retry, log to a file, notify admins
//     });

